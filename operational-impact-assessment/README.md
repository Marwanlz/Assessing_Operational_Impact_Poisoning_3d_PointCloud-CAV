# Impact Assessment Tool

### Install Docker et Docker-Compose

### Build the associated docker image

sudo docker build -t bia .

### Start the service

sudo docker-compose up

### Test the tool

Open a web browser and go to http://127.0.0.1:8003 to access the tool's GUI.
